# 重启电脑后，Kimi Code CLI 的聊天记录还在吗？—— `kimi -c` 会话恢复技巧

**结论先说：在，丢不了。** 聊天会话保存在本地磁盘上，不是只在内存里。重启 Mac、关掉终端都不影响，随时可以接着上次的对话继续聊。

## 两个命令

### `kimi -c`：接着上次继续

```bash
kimi -c
```

`-c` 是 `--continue` 的缩写，意思是"继续**当前目录**下最近一次的会话"。

- 在终端直接敲 `kimi` → 开一个**全新对话**，没有之前的上下文
- 敲 `kimi -c` → 把上次在这个目录里的对话**完整加载回来**，AI 记得之前聊过的所有内容

**注意点：`-c` 是按"工作目录"找的。** 如果你之前在 `~/code/foo` 里聊的，就要先 `cd ~/code/foo` 再 `kimi -c`，才能接上对应的那段对话。

### `kimi -S`：从历史会话里挑一个

```bash
kimi -S
```

当历史会话有多个、记不清哪个是哪个时，用这个命令。它会列出会话清单，让你交互式地选择要恢复哪一个。也可以 `kimi -S <会话ID>` 直接指定。

## 一个常见误区：聊天恢复 ≠ 终端 tab 恢复

重启后经常出现这种情况：终端（比如 Otty、Warp）把之前的 tab 恢复出来了，但里面只是一个新的 shell，之前聊到一半的内容"不见了"。

其实这是**两套独立的机制**，各管各的：

| 恢复什么 | 谁负责 | 怎么恢复 |
|---------|--------|---------|
| 聊天内容 | Kimi Code CLI 自己 | `kimi -c` / `kimi -S` |
| 终端窗口、tab、tab 自定义名 | 终端 App（Otty / Warp 等） | 终端自己的会话恢复设置 |

所以"终端重启后 tab 里聊天没了"不是聊天丢了，只是 tab 被恢复成了一个普通 shell——在这个 shell 里敲 `kimi -c`，聊天就回来了。

## 为什么这个技巧在任何终端都通用

因为会话恢复是 **kimi 的功能，不是终端的功能**。会话数据存在本地（`~/.kimi-code` 相关目录下），跟你在哪个终端里运行无关。

Otty、Warp、iTerm2、macOS 自带终端——`kimi -c` 和 `kimi -S` 的用法完全一样。之前以为"Warp 重启后只能重新开"，其实只是不知道这个参数而已。

## 实用场景

- **重启系统前**：放心重启，回来 `kimi -c` 接着干活
- **误关了终端窗口**：重开终端，`cd` 到原目录，`kimi -c`
- **同时推进多件事**：每个项目目录各聊各的，用 `kimi -S` 在会话之间切换
