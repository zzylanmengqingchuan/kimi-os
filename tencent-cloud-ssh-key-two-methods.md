# 腾讯云 SSH 密钥免密登录：两种方式的区别与"消失的私钥"坑

> 实战环境：腾讯云轻量应用服务器（广州），Ubuntu，用户 `ubuntu`。
> 核心坑：**控制台创建密钥时，私钥是浏览器静默自动下载的，没有"下载按钮"，事后永远无法再次下载。**

## 背景：两种免密登录方式

SSH 密钥 = 一把锁配一把钥匙：**公钥是锁**（装在服务器 `~/.ssh/authorized_keys`，不怕泄露），**私钥是钥匙**（留在本机，丢了别人就能进你的服务器）。

| | 方式一：本机生成 | 方式二：控制台生成 |
| --- | --- | --- |
| 密钥对在哪里生成 | 自己的电脑（`ssh-keygen`，如 ed25519） | 腾讯云的服务器（RSA 2048） |
| 私钥出过门吗 | 没有，最安全（最佳实践） | 从云端经网络下载到本机，多一次暴露机会 |
| 公钥怎么上服务器 | 手动装进 `authorized_keys`（需先能登录） | 控制台「绑定实例」自动注入 |
| 私钥丢了怎么办 | 重新生成一对，重装公钥 | 无法找回，只能删除密钥重建 |
| 适合场景 | 已能登录服务器 | 还没有任何登录手段的冷启动 |

两种方式机制相同、效果相同，且**可以同时有效**——`authorized_keys` 支持多把公钥，控制台绑定是追加不是覆盖。

## 坑：创建密钥后找不到私钥下载入口

腾讯云控制台「SSH 密钥」列表的操作列只有「绑定实例 / 解绑实例 / 编辑标签 / 删除」，没有任何下载按钮。官方文档说明：

> 创建完成后将**自动下载**私钥，腾讯云不会保管您的私钥信息。私钥**仅有一次下载机会**，并将自动保存至您浏览器的默认下载地址。

即：点「确定」创建的瞬间，浏览器**静默**把 `.pem` 下载到了 `~/Downloads`。没注意到就等于"丢了"，只能删了重建。

> 对比：阿里云是创建后弹窗里有明显的「下载私钥」按钮；腾讯云是直接触发浏览器下载，无任何提示。

## 标准操作流程（控制台方式）

```bash
# 1. 控制台创建密钥(自动下载 .pem 到 ~/Downloads)并「绑定实例」
# 2. 把私钥安置到 ~/.ssh,收紧权限(必须 600,否则 ssh 拒用)
cp ~/Downloads/test.pem ~/.ssh/test.pem
chmod 600 ~/.ssh/test.pem

# 3. 登录验证
ssh -i ~/.ssh/test.pem ubuntu@<服务器公网IP> 'whoami'
```

## 两个易混淆点

**1. 网页终端能登录 ≠ 本机 ssh 能登录。** 控制台的 OrcaTerm / VNC 网页终端走腾讯云后端认证，和本机有没有私钥无关——任何电脑开浏览器都能登。本机 ssh 免密才是自动化部署需要的那条路。

**2. 不需要把密钥"交给" AI。** Agent 跑在本机，执行 `ssh` 用的就是 `~/.ssh/` 里的密钥。密钥放进 `~/.ssh/` 那一刻，人和 Agent 都能用了，不存在把私钥发给云端 AI 的步骤。

## 注意

- 绑定密钥后，腾讯云默认**禁止 root 密码登录**（安全设计），钥匙务必保管好。
- 验证"能不能登录"最快的方式就是实测一把，别靠推测。
